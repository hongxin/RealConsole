# v1.52.0 剩余实施指南（30%）

**当前进度**: 75% (ImageHistoryEntry 已添加)
**剩余工作**: Session 集成 + WebSocket + 前端
**预计时间**: 1-2 小时

---

## ✅ 已完成（75%）

1. PNG 导出修复（动画处理）
2. ViewImage 工具（后端）
3. ImageData 结构
4. ServerMessage::Image
5. **ImageHistoryEntry 结构** ✅ 刚完成

---

## 🚧 剩余任务（25%）

### Task 1: Session 结构集成（15分钟）

#### 1.1 添加 image_history 字段

**文件**: `src/web/session.rs`
**位置**: 搜索 `pub chart_history:` (约 line 535)

```rust
// 找到这行
pub chart_history: Arc<RwLock<Vec<ChartHistoryEntry>>>,

// 在它后面添加
/// v1.52.0: 图像历史记录
pub image_history: Arc<RwLock<Vec<ImageHistoryEntry>>>,
```

#### 1.2 Session::new() 初始化

**位置**: 在 Session::new() 方法中，找到 chart_history 初始化的地方 (约 line 560)

```rust
// 找到这行
chart_history: Arc::new(RwLock::new(Vec::new())),

// 在它后面添加
image_history: Arc::new(RwLock::new(Vec::new())),
```

#### 1.3 添加 add_image_to_history() 方法

**位置**: 在 `add_chart_to_history()` 方法后面添加 (约 line 590)

```rust
/// v1.52.0: 添加图像到历史记录
pub async fn add_image_to_history(
    &self,
    image_data: visualization::ImageData,
    round_id: Option<String>,
    command: String,
) -> ImageHistoryEntry {
    let entry = ImageHistoryEntry::new(image_data, round_id, command);
    let mut history = self.image_history.write().await;
    history.push(entry.clone());
    entry
}
```

---

### Task 2: SerializableSession 集成（10分钟）

#### 2.1 添加导入

**文件**: `src/web/session_manager.rs`
**位置**: Line 5 (修改现有导入)

```rust
// 修改这行
use crate::web::session::ConversationRound;

// 改为
use crate::web::session::{ChartHistoryEntry, ImageHistoryEntry, ConversationRound};
```

#### 2.2 SerializableSession 添加字段

**位置**: 在 chart_history 字段后面 (约 line 37)

```rust
// 找到
pub chart_history: Vec<ChartHistoryEntry>,

// 在它后面添加
/// v1.52.0: 图像历史记录
#[serde(default, skip_serializing_if = "Vec::is_none")]
pub image_history: Vec<ImageHistoryEntry>,
```

#### 2.3 更新 to_serializable()

**文件**: `src/web/session.rs`
**位置**: to_serializable() 方法中 (约 line 740)

```rust
// 找到
let chart_history = self.chart_history.read().await.clone();

// 在它后面添加
let image_history = self.image_history.read().await.clone();

// 然后在 SerializableSession 构造中添加
SerializableSession {
    // ... 其他字段 ...
    chart_history,
    image_history,  // 添加这行
    // ...
}
```

#### 2.4 更新 from_serializable()

**位置**: from_serializable() 方法中，在 chart_history 恢复后 (约 line 780)

```rust
// 找到这段
{
    let mut chart_history = session.chart_history.write().await;
    *chart_history = serializable.chart_history;
}

// 在它后面添加
// v1.52.0: 恢复图像历史
{
    let mut image_history = session.image_history.write().await;
    *image_history = serializable.image_history;
}
```

---

### Task 3: WebSocket 处理（30分钟）

#### 3.1 tool_executor.rs 拦截

**文件**: `src/tool_executor.rs`
**位置**: 在检测 `__CHART_DATA__` 后添加 (约 line 340)

```rust
// 找到检测图表数据的代码
let mut chart_data = None;
for result in &tool_results {
    if result.content.starts_with("__CHART_DATA__:") {
        chart_data = Some(result.content.clone());
        break;
    }
}

// 在它后面添加图像数据检测
// v1.52.0: 检测图像数据标记
let mut image_data = None;
for result in &tool_results {
    if result.content.starts_with("__IMAGE_DATA__:") {
        image_data = Some(result.content.clone());
        break;
    }
}
```

**位置**: 在返回 chart_data 的地方后面添加 (约 line 380)

```rust
// 找到
if let Some(chart_json) = chart_data {
    let debug_info = Self::encode_debug_info(&conversation_rounds);
    return Ok(format!("{}__CHART__{}__DEBUG__{}",
        "✅ 图表已生成",
        chart_json,
        debug_info
    ));
}

// 在它后面添加
// v1.52.0: 如果检测到 ImageData
if let Some(image_json) = image_data {
    let debug_info = Self::encode_debug_info(&conversation_rounds);
    return Ok(format!("{}__IMAGE__{}__DEBUG__{}",
        "✅ 图像已加载",
        image_json,
        debug_info
    ));
}
```

#### 3.2 websocket.rs 提取函数

**文件**: `src/web/websocket.rs`
**位置**: 在 Chart 处理函数后面添加 (约 line 2620)

```rust
/// v1.52.0: 提取并处理图像数据
fn extract_and_process_image_data(content: &str) -> (String, Option<visualization::ImageData>) {
    if let Some(start) = content.find("__IMAGE__") {
        let json_start = start + "__IMAGE__".len();
        if let Some(end) = content[json_start..].find("__DEBUG__") {
            let json_str = &content[json_start..json_start + end];

            // 直接解析为 ImageData
            if let Ok(image_data) = serde_json::from_str::<visualization::ImageData>(json_str) {
                // 验证数据
                if let Err(e) = image_data.validate() {
                    log::error!("[v1.52.0] ImageData 验证失败: {}", e);
                    return (content.to_string(), None);
                }

                // 清理内容（移除 __IMAGE__ 标记）
                let clean_content = format!(
                    "{}{}",
                    &content[..start].trim_end(),
                    &content[json_start + end..]
                );

                return (clean_content, Some(image_data));
            }
        }
    }
    (content.to_string(), None)
}
```

#### 3.3 execute_llm_chat() 中添加处理

**位置**: 在 Chart 处理后面添加 (约 line 1110)

```rust
// 找到图表处理
let (final_content, chart_data_opt) = extract_and_process_chart_data(&clean_content);

// 在它后面添加图像处理
// v1.52.0: 提取并处理图像数据
let (final_content, image_data_opt) = extract_and_process_image_data(&final_content);

// 在发送 Chart 消息后添加
if let Some(image_data) = image_data_opt {
    // 添加到会话的图像历史
    session.add_image_to_history(
        image_data.clone(),
        Some(round_id.clone()),
        user_message.clone()
    ).await;

    // 发送 Image 消息
    let image_msg = ServerMessage::Image {
        round_id: round_id.clone(),
        image_data,
    };
    sender.send(Message::Text(serde_json::to_string(&image_msg)?)).await?;
}
```

#### 3.4 handle_load_session() 添加图像恢复

**位置**: 在发送 Chart 消息循环后 (约 line 2190)

```rust
// 找到这段
// v1.51.0: 为历史会话中的每个图表发送 Chart 消息
for chart_entry in &serializable.chart_history {
    // ...
}

// 在它后面添加
// v1.52.0: 为历史会话中的每个图像发送 Image 消息
for image_entry in &serializable.image_history {
    if let Some(ref round_id) = image_entry.round_id {
        let image_msg = ServerMessage::Image {
            round_id: round_id.clone(),
            image_data: image_entry.image_data.clone(),
        };
        sender
            .send(Message::Text(serde_json::to_string(&image_msg)?))
            .await?;
    }
}
```

---

### Task 4: 前端实现（30分钟）

#### 4.1 添加追踪器

**文件**: `src/web/frontend.rs`
**位置**: Terminal 构造函数中 (约 line 1285)

```javascript
// 找到
this.chartDataByRound = {};

// 在它后面添加
this.imageDataByRound = {};  // v1.52.0: { round_id: image_data }
```

#### 4.2 添加消息处理

**位置**: onMessage 方法中 (约 line 1460)

```javascript
// 找到
case 'chart':
    this.renderChart(msg);
    break;

// 在它后面添加
case 'image':
    this.renderImage(msg);
    break;
```

#### 4.3 实现 renderImage()

**位置**: renderChart() 后面 (约 line 3120)

```javascript
renderImage(msg) {
    console.log(`[v1.52.0] Rendering image for round: ${msg.round_id}`);
    const imageData = msg.image_data;

    // 保存图像数据到追踪器
    this.imageDataByRound[msg.round_id] = imageData;

    // 找到 Round 卡片
    const roundElement = document.getElementById(`round-${msg.round_id}`);
    if (!roundElement) {
        console.error(`[v1.52.0 ERROR] Round not found: ${msg.round_id}`);
        return;
    }

    const outputContent = roundElement.querySelector('.output-content');
    if (!outputContent) {
        console.error(`[v1.52.0 ERROR] Output content not found`);
        return;
    }

    // 创建图像容器
    const imageCard = document.createElement('div');
    imageCard.className = 'image-card';

    // 创建图像元素
    const img = document.createElement('img');
    img.className = 'display-image';
    img.alt = imageData.alt_text || '图像';

    // 设置图像源
    if (imageData.image_type === 'base64') {
        img.src = `data:${imageData.mime_type};base64,${imageData.data}`;
    } else if (imageData.image_type === 'url') {
        img.src = imageData.data;
    }

    // 加载处理
    img.onload = () => imageCard.classList.add('loaded');
    img.onerror = () => {
        imageCard.classList.add('error');
        const errorMsg = document.createElement('div');
        errorMsg.className = 'image-error';
        errorMsg.textContent = `图像加载失败: ${imageData.filename || ''}`;
        imageCard.appendChild(errorMsg);
    };

    imageCard.appendChild(img);

    // 添加文件名说明
    if (imageData.filename) {
        const caption = document.createElement('div');
        caption.className = 'image-caption';
        let text = imageData.filename;
        if (imageData.size_bytes) {
            const sizeMB = (imageData.size_bytes / (1024 * 1024)).toFixed(2);
            text += ` (${sizeMB} MB)`;
        }
        caption.textContent = text;
        imageCard.appendChild(caption);
    }

    outputContent.appendChild(imageCard);
    this.scrollToBottom();
}
```

#### 4.4 添加 CSS 样式

**位置**: 样式区域 (约 line 800)

```css
.image-card {
    margin: 1rem 0;
    border-radius: 8px;
    overflow: hidden;
    background: var(--card-bg);
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
}

.display-image {
    max-width: 100%;
    height: auto;
    display: block;
    cursor: zoom-in;
    transition: transform 0.3s;
}

.display-image:hover {
    transform: scale(1.02);
}

.image-caption {
    padding: 0.75rem;
    font-size: 0.9rem;
    color: var(--text-secondary);
    border-top: 1px solid var(--border-color);
}

.image-error {
    padding: 2rem;
    text-align: center;
    color: var(--error-color);
}
```

#### 4.5 localStorage 持久化

**位置**: saveCurrentSession() (约 line 3595)

```javascript
// 找到
charts: this.chartDataByRound,

// 在它后面添加
images: this.imageDataByRound,  // v1.52.0
```

**位置**: restoreSession() (约 line 3640)

```javascript
// 找到
this.chartDataByRound = session.charts || {};

// 在它后面添加
this.imageDataByRound = session.images || {};

// 在重新渲染图表后添加
// v1.52.0: 重新渲染图像
if (this.imageDataByRound && Object.keys(this.imageDataByRound).length > 0) {
    console.log('[v1.52.0] Restoring images:', Object.keys(this.imageDataByRound).length);
    for (const [round_id, image_data] of Object.entries(this.imageDataByRound)) {
        this.renderImage({ round_id, image_data });
    }
}
```

**位置**: clearAll() (约 line 1775)

```javascript
// 找到
this.chartDataByRound = {};

// 在它后面添加
this.imageDataByRound = {};  // v1.52.0
```

---

## 🧪 测试流程

### 1. 编译测试
```bash
cargo build --release
```

### 2. 功能测试

#### 测试 1: 基础图像显示
```bash
# 准备测试图片
cp some_image.png /tmp/test.png

# 启动服务
./target/release/realconsole web --port 7788

# 在浏览器中输入
view /tmp/test.png
```

**验证点**:
- ✅ 图像正确显示
- ✅ 文件名显示
- ✅ 大小信息显示

#### 测试 2: 会话保存/加载
```bash
1. 显示图像
2. 点击"保存会话"
3. 刷新页面
4. 点击"加载会话"
5. 验证图像恢复
```

#### 测试 3: 浏览器刷新
```bash
1. 显示图像
2. 按 F5 刷新
3. 验证图像恢复（localStorage）
```

---

## 📝 实施顺序建议

按以下顺序实施，每步完成后编译测试：

1. **Session 集成** → 编译测试
2. **WebSocket 处理** → 编译测试
3. **前端渲染** → 完整测试

---

## 🎯 完成标志

所有任务完成后，应该实现：
- ✅ 用户输入 `view /path/to/image.png`
- ✅ 图像在浏览器中显示
- ✅ 保存会话后可恢复
- ✅ 刷新浏览器后可恢复

---

**预计总时间**: 1-2 小时
**当前进度**: 75%
**最后更新**: 2025-01-23
